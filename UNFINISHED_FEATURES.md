# 📋 Отчет о незавершенных функциях и фичах

**Дата анализа:** 2025-01-27  
**Версия проекта:** SportEvents Manager

---

## 🔴 Критические незавершенные функции

### 1. **Присоединение к учебному заведению**
**Файл:** `lib/features/institutions/presentation/pages/join_institution_page.dart:191`

**Статус:** TODO - только заглушка с SnackBar

**Описание:**
```dart
// TODO: Реализовать присоединение к учебному заведению
ScaffoldMessenger.of(context).showSnackBar(
  const SnackBar(
    content: Text('Функционал присоединения будет реализован позже'),
  ),
);
```

**Что нужно сделать:**
- Реализовать метод в `InstitutionRepository` для добавления пользователя в заведение
- Обновить список `adminIds` или создать отдельную коллекцию участников
- Добавить проверку прав доступа
- Обновить UI после успешного присоединения

---

### 2. **Создание турнирной сетки**
**Файл:** `lib/features/matches/presentation/pages/tournament_bracket_page.dart:196`

**Статус:** TODO - только заглушка

**Описание:**
```dart
// TODO: Implement bracket creation
ScaffoldMessenger.of(context).showSnackBar(
  const SnackBar(
    content: Text('Турнирная сетка создана!'),
    backgroundColor: AppTheme.successColor,
  ),
);
```

**Что нужно сделать:**
- Использовать `TournamentService.createSingleEliminationBracket()` из реальных команд события
- Заменить mock team IDs (`team-1`, `team-2`) на реальные ID команд из события
- Сохранить сетку в Firestore через репозиторий
- Обновить состояние события

**Связанные файлы:**
- `lib/features/tournament/domain/services/tournament_service.dart` - сервис уже реализован
- `lib/features/events/data/repositories/sport_event_repository_impl.dart` - нужно добавить метод сохранения сетки

---

### 3. **Обновление счета матча**
**Файл:** `lib/features/matches/presentation/pages/tournament_bracket_page.dart:250`

**Статус:** TODO - только заглушка

**Описание:**
```dart
// TODO: Implement score update
ScaffoldMessenger.of(context).showSnackBar(
  const SnackBar(
    content: Text('Счет обновлен!'),
    backgroundColor: AppTheme.successColor,
  ),
);
```

**Что нужно сделать:**
- Использовать `TournamentService.completeMatch()` для обновления счета
- Получить реальный ID администратора из `authStateProvider` (не mock)
- Сохранить изменения в Firestore
- Обновить UI после сохранения
- Обработать продвижение победителей в следующий раунд

**Связанные файлы:**
- `lib/features/tournament/presentation/notifiers/tournament_notifier.dart` - есть метод `completeMatch()`, но нужно проверить интеграцию

---

### 4. **Начало турнира**
**Файл:** `lib/features/teams/presentation/pages/team_formation_page.dart:363`

**Статус:** TODO - только заглушка

**Описание:**
```dart
// TODO: Implement tournament start logic
ScaffoldMessenger.of(context).showSnackBar(
  const SnackBar(
    content: Text('Турнир начался!'),
    backgroundColor: AppTheme.successColor,
  ),
);
```

**Что нужно сделать:**
- Валидация: минимум 2 команды, минимум 4 участника
- Создать турнирную сетку через `TournamentService.createSingleEliminationBracket()`
- Обновить статус события на `EventStatus.running`
- Сохранить сетку в Firestore
- Перейти на экран турнирной сетки

**Примечание:** В `event_lobby_page.dart:296` есть частичная реализация `_startTournament()`, но она только меняет статус, не создает сетку.

---

### 5. **Firebase Initialization Error**
**Файл:** `lib/core/di/dependency_injection.dart:10`

**Статус:** UnimplementedError

**Описание:**
```dart
final firebaseAppProvider = Provider<FirebaseApp>((ref) {
  throw UnimplementedError('Firebase must be initialized');
});
```

**Что нужно сделать:**
- Реализовать провайдер, который возвращает инициализированный `FirebaseApp`
- Убедиться, что `initializeFirebase()` вызывается в `main.dart` перед запуском приложения
- Проверить, что `firebaseAppProvider` используется корректно (или удалить, если не нужен)

---

## 🟡 Частично реализованные функции

### 6. **Страница деталей команды**
**Файл:** `lib/features/teams/presentation/pages/team_details_page.dart`

**Статус:** Только заглушка

**Описание:**
Страница существует, но показывает только текст "Детали команды: $teamId"

**Что нужно сделать:**
- Реализовать UI для отображения:
  - Название команды
  - Список участников с их ролями
  - Статистика команды (матчи, победы, поражения)
  - История матчей
- Добавить возможность редактирования команды (для администратора)
- Добавить возможность удаления участников (для капитана/администратора)
- Получать данные из Firestore, а не из mock данных

**Связанные файлы:**
- `lib/features/teams/presentation/pages/teams_page.dart:303` - навигация уже реализована (`context.push('/teams/$teamId')`)
- Нужно добавить роут в `app_router.dart`

---

### 7. **Страница деталей матча**
**Файл:** `lib/features/matches/presentation/pages/match_details_page.dart`

**Статус:** Только заглушка

**Описание:**
Страница существует, но показывает только текст "Детали матча: $matchId"

**Что нужно сделать:**
- Реализовать UI для отображения:
  - Информация о командах
  - Текущий счет
  - Статус матча
  - История изменений счета
  - Кнопка для обновления счета (для администратора)
- Добавить навигацию к этой странице из турнирной сетки

---

### 8. **Шаринг результатов турнира**
**Файл:** `lib/features/matches/presentation/pages/tournament_bracket_page.dart:267`

**Статус:** TODO - только заглушка

**Описание:**
```dart
// TODO: Implement sharing results
ScaffoldMessenger.of(context).showSnackBar(
  const SnackBar(
    content: Text('Результаты скопированы в буфер обмена!'),
    backgroundColor: AppTheme.successColor,
  ),
);
```

**Что нужно сделать:**
- Сформировать текстовое представление результатов турнира
- Использовать `Share.share()` для шаринга
- Добавить опцию экспорта в различные форматы (текст, изображение)

---

## 🟢 Mock данные, которые нужно заменить

### 9. **Использование Uuid().v4() вместо реальных ID пользователей**

**Места использования:**
- `lib/features/events/data/repositories/sport_event_repository_impl.dart:36` - создание события
- `lib/features/events/data/repositories/sport_event_repository_impl.dart:185, 211, 251` - создание команд
- `lib/features/institutions/presentation/notifiers/institutions_notifier.dart:101` - создание заведения
- `lib/features/tournament/domain/services/tournament_service.dart:34, 60` - создание матчей и сетки

**Что нужно сделать:**
- Получать ID текущего пользователя из `authStateProvider`
- Использовать реальный ID для полей `createdBy`, `startedBy`, `recordedBy`
- Убрать все вызовы `Uuid().v4()` для пользовательских ID

**Связанные файлы:**
- `lib/features/auth/presentation/notifiers/auth_controller.dart` - проверить, что `authStateProvider` возвращает реального пользователя

---

### 10. **Mock данные для команд**

**Файл:** `lib/features/teams/presentation/pages/teams_page.dart:262-301`

**Описание:**
```dart
List<TeamMember> _getTeamMembers(String teamId, dynamic event) {
  // Моковые данные для демонстрации
  final mockMembers = <TeamMember>[];
  
  if (teamId == 'team-1') {
    // Первая группа - админ и участник
    mockMembers.addAll([...]);
  } else if (teamId == 'team-2') {
    // Вторая группа - только участник
    mockMembers.add(...);
  }
  
  return mockMembers;
}
```

**Что нужно сделать:**
- Получать реальных участников команды из Firestore
- Использовать данные из `event.teams[teamId]` или отдельной коллекции `team_members`
- Убрать проверки на `team-1` и `team-2`

**Связанные файлы:**
- `lib/core/mock/mock_data.dart:49-50, 73-74, 94-95` - также использует `team-1` и `team-2`

---

### 11. **Mock данные в Tournament Bracket**

**Файл:** `lib/core/mock/mock_data.dart:94-95`

**Описание:**
```dart
teamAId: 'team-1',
teamBId: 'team-2',
```

**Что нужно сделать:**
- При создании турнирной сетки использовать реальные ID команд из события
- Убедиться, что `TournamentService.createSingleEliminationBracket()` получает реальные `teamIds`

---

## 🔵 Отсутствующие роуты

### 12. **Роут для деталей команды**

**Файл:** `lib/core/navigation/app_router.dart`

**Проблема:**
В `teams_page.dart:310` используется `context.push('/teams/$teamId')`, но роут не определен в `app_router.dart`

**Что нужно сделать:**
Добавить роут:
```dart
GoRoute(
  path: '/teams/:teamId',
  name: 'teamDetails',
  builder: (context, state) {
    final teamId = state.pathParameters['teamId']!;
    return TeamDetailsPage(teamId: teamId);
  },
),
```

---

### 13. **Роут для деталей матча**

**Файл:** `lib/core/navigation/app_router.dart`

**Проблема:**
Страница `MatchDetailsPage` существует, но роут не определен

**Что нужно сделать:**
Добавить роут:
```dart
GoRoute(
  path: '/matches/:matchId',
  name: 'matchDetails',
  builder: (context, state) {
    final matchId = state.pathParameters['matchId']!;
    return MatchDetailsPage(matchId: matchId);
  },
),
```

---

## 🟣 Улучшения и доработки

### 14. **QR Scanner интеграция**

**Статус:** Реализован, но нужно проверить

**Файлы:**
- `lib/features/events/presentation/widgets/qr_scanner_widget.dart` - виджет реализован
- `lib/features/events/presentation/pages/join_event_page.dart:472` - интеграция есть

**Что проверить:**
- Разрешения на камеру запрашиваются корректно
- Обработка ошибок работает
- Сканирование останавливается после успешного результата
- UI корректно обновляется после сканирования

---

### 15. **Active Games Refresh**

**Файл:** `lib/features/games/presentation/pages/active_games_page.dart:84-87`

**Статус:** Реализован через `RefreshIndicator`

**Описание:**
```dart
RefreshIndicator(
  onRefresh: () async {
    ref.invalidate(activeGamesNotifierProvider);
  },
  ...
)
```

**Что можно улучшить:**
- Добавить индикатор загрузки при обновлении
- Добавить обработку ошибок при обновлении
- Оптимизировать частоту обновления (не слишком часто)

**Примечание:** По ROADMAP.md это было помечено как TODO, но сейчас реализовано.

---

## 📊 Сводная таблица

| # | Функция | Приоритет | Статус | Файл |
|---|---------|-----------|--------|------|
| 1 | Присоединение к учебному заведению | 🔴 Высокий | TODO | `join_institution_page.dart:191` |
| 2 | Создание турнирной сетки | 🔴 Высокий | TODO | `tournament_bracket_page.dart:196` |
| 3 | Обновление счета матча | 🔴 Высокий | TODO | `tournament_bracket_page.dart:250` |
| 4 | Начало турнира | 🔴 Высокий | TODO | `team_formation_page.dart:363` |
| 5 | Firebase Initialization | 🔴 Высокий | UnimplementedError | `dependency_injection.dart:10` |
| 6 | Страница деталей команды | 🟡 Средний | Заглушка | `team_details_page.dart` |
| 7 | Страница деталей матча | 🟡 Средний | Заглушка | `match_details_page.dart` |
| 8 | Шаринг результатов | 🟡 Средний | TODO | `tournament_bracket_page.dart:267` |
| 9 | Замена Uuid().v4() на реальные ID | 🟡 Средний | Mock | Множество файлов |
| 10 | Mock данные команд | 🟡 Средний | Mock | `teams_page.dart:262` |
| 11 | Mock данные в сетке | 🟡 Средний | Mock | `mock_data.dart:94-95` |
| 12 | Роут деталей команды | 🟢 Низкий | Отсутствует | `app_router.dart` |
| 13 | Роут деталей матча | 🟢 Низкий | Отсутствует | `app_router.dart` |
| 14 | QR Scanner | 🟢 Низкий | Реализован | Проверить |
| 15 | Active Games Refresh | 🟢 Низкий | Реализован | Улучшить |

---

## 🎯 Рекомендуемый порядок реализации

1. **Firebase Initialization** (критично для работы приложения)
2. **Замена Uuid().v4() на реальные ID** (основа для остальных функций)
3. **Создание турнирной сетки** (использует реальные команды)
4. **Обновление счета матча** (использует реальные ID)
5. **Начало турнира** (объединяет создание сетки и обновление статуса)
6. **Присоединение к учебному заведению**
7. **Страница деталей команды** + роут
8. **Страница деталей матча** + роут
9. **Шаринг результатов**
10. **Удаление всех mock данных**

---

## 📝 Примечания

- Все TODO комментарии найдены через grep по паттерну `TODO|FIXME|HACK|XXX`
- Все UnimplementedError найдены через grep по паттерну `UnimplementedError`
- Mock данные найдены через поиск по `team-1`, `team-2`, `mock`, `hardcoded`
- QR Scanner уже реализован, но требует проверки интеграции
- Active Games Refresh реализован, но может быть улучшен

---

**Следующие шаги:**
1. Создать задачи в системе управления проектом для каждого пункта
2. Определить приоритеты в зависимости от бизнес-требований
3. Начать реализацию с критических функций
4. Обновлять этот документ по мере выполнения задач

